import Foundation

enum GameAssets {

    enum Background {
        static let main = "gameBackground"
    }

    enum Boat {
        static let main = "boatSprite"
    }

    enum Character {
        static let student = "character_student"
        static let janitor = "character_janitor"
        static let gardener = "character_gardener"
    }
}
